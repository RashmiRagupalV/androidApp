<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="redflag.capstone.com.redflag.LoginSuccessActivity">

  <!--  <TextView
        android:id="@+id/greetingUser"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/student_list"
        android:linksClickable="true"
        android:onClick="studentListBySchool"/>

    <SearchView
        android:id="@+id/studentSearch"
        android:text="@string/hello_world"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/greetingUser"
        android:queryHint="@string/student_name"/>

    <Button
        android:id="@+id/searchStudent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/button_search"
        android:layout_toRightOf="@+id/studentSearch"
        android:layout_below="@+id/greetingUser"
        android:onClick="searchStudent"/>

    <TextView
        android:id="@+id/studentListBySchool"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/student_list"
        android:linksClickable="true"
        android:layout_below="@+id/studentSearch"
        android:onClick="studentListBySchool"/>-->

</RelativeLayout>
